# AI美食顾问系统 - 面试级项目升级规划

## 一、项目定位

**升级后的项目名称**: 智能美食推荐与餐厅管理系统（AI-Powered Food Recommendation & Restaurant Management System）

**项目亮点**:
- 结合AI大模型的实际业务应用（LangChain4j + 通义千问）
- 完整的微服务架构（可选单体或微服务）
- 主流中间件技术栈
- 高并发场景设计
- 完善的工程化实践

---

## 二、可实现的核心需求

### 2.1 用户端功能（C端）

#### ✅ 已有功能
- [x] AI聊天对话（流式响应）
- [x] 餐厅预订

#### 🆕 新增功能

**1. 用户体系**
- 用户注册/登录（手机号+验证码、微信登录）
- 用户信息管理（昵称、头像、饮食偏好）
- 用户标签系统（口味偏好、过敏源、饮食习惯）
- 多端登录态管理（JWT + Redis）

**2. 智能推荐系统**
- 基于用户画像的个性化推荐
- 基于地理位置的附近餐厅推荐
- 基于协同过滤的"猜你喜欢"
- 实时热门餐厅排行榜
- AI美食知识问答（基于RAG）

**3. 订单与评价系统**
- 餐厅预订订单管理（创建、查询、取消、修改）
- 订单状态流转（待确认、已确认、已完成、已取消）
- 用户评价与打分系统
- 评价内容审核（敏感词过滤）
- 评价图片上传（OSS）

**4. 餐厅信息管理**
- 餐厅详情查询（菜品、环境、价格）
- 餐厅收藏夹
- 餐厅搜索（多条件：地区、菜系、价格、评分）
- 热门菜品推荐

**5. 社交功能（可选）**
- 美食动态发布（图文）
- 动态点赞、评论
- 关注好友、查看好友推荐

### 2.2 管理端功能（B端）

**1. 商家管理**
- 餐厅入驻审核
- 餐厅信息管理（菜品、环境图、营业时间）
- 预订订单管理
- 营业数据统计（订单量、评分趋势）

**2. 平台管理**
- 用户管理（封禁、信息查询）
- 评价审核管理
- 系统配置管理
- 数据统计大屏（今日订单、活跃用户、热门餐厅）

**3. AI模型管理**
- RAG知识库管理（文档上传、删除、更新）
- AI对话日志查看
- 模型效果监控（响应时间、token消耗）
- 敏感词库维护

---

## 三、技术栈升级方案

### 3.1 核心框架（必选）

| 技术 | 版本 | 用途 | 面试加分点 |
|------|------|------|-----------|
| **Spring Boot** | 3.5.9 | 基础框架 | 熟悉最新版本特性 |
| **Spring Security** | 6.x | 认证授权 | 理解RBAC、OAuth2、JWT |
| **MyBatis-Plus** | 3.5.x | ORM增强 | 替代原生MyBatis，提升开发效率 |
| **Spring Data Redis** | ✅ 已有 | 缓存 | 缓存设计模式、分布式锁 |
| **Spring Validation** | 内置 | 参数校验 | 统一异常处理 |

### 3.2 中间件技术栈（重点）

#### 🔥 必须使用（高频面试点）

**1. Redis（缓存 + 分布式能力）**
```
✅ 已有用途：聊天记忆、向量数据库
🆕 新增用途：
- 用户登录态存储（Token + 用户信息）
- 接口限流（滑动窗口算法）
- 热点数据缓存（餐厅信息、热门榜单）
- 分布式锁（预订库存扣减）
- 延迟队列（订单超时自动取消）
- 发布订阅（实时消息推送）
```
**面试考点**: 缓存穿透/击穿/雪崩、缓存一致性、Redis数据结构、持久化、主从复制

**2. MySQL（主数据库）**
```
✅ 已有：restaurant_reservation表
🆕 新增表设计：
- 用户表（user）
- 餐厅表（restaurant）
- 订单表（order）
- 评价表（review）
- 菜品表（dish）
- 用户标签表（user_tag）
等...完整的表结构设计
```
**面试考点**: 索引优化、事务隔离级别、慢SQL优化、分库分表（如果数据量大）

**3. RabbitMQ / RocketMQ（消息队列）**
```
🆕 使用场景：
- 订单异步处理（下单后发送确认短信/邮件）
- 评价内容异步审核
- 用户行为日志异步入库
- 削峰填谷（秒杀场景）
- 延迟消息（订单超时取消）
```
**面试考点**: 消息可靠性、幂等性、消息堆积、死信队列

**4. Elasticsearch（搜索引擎）**
```
🆕 使用场景：
- 餐厅搜索（支持全文检索、拼音搜索、模糊搜索）
- 菜品搜索
- 用户动态搜索
- 热门关键词统计
```
**面试考点**: 倒排索引、分词器、相关性算分、聚合查询

#### 🌟 推荐使用（加分项）

**5. Sentinel（流量控制）**
```
🆕 使用场景：
- API限流（QPS限制）
- 熔断降级（AI接口超时降级）
- 热点参数限流（热门餐厅查询）
```

**6. XXL-Job / Elastic-Job（分布式定时任务）**
```
🆕 使用场景：
- 每日热门榜单定时计算
- 订单超时自动取消
- 过期预订清理
- AI知识库定时更新
```

**7. Nginx（反向代理）**
```
🆕 使用场景：
- 负载均衡
- 静态资源托管
- 接口限流
- HTTPS配置
```

**8. MinIO / 阿里云OSS（对象存储）**
```
🆕 使用场景：
- 用户头像上传
- 评价图片上传
- 餐厅环境照片存储
- AI知识库文档存储
```

#### 🎯 可选（技术亮点）

**9. Redisson（分布式场景）**
```
- 分布式锁（预订扣减库存）
- 布隆过滤器（防止缓存穿透）
- 限流器（RateLimiter）
```

**10. Nacos（微服务架构）**
```
如果升级为微服务架构：
- 服务注册发现
- 配置中心
- 动态配置刷新
```

### 3.3 工具类库

| 技术 | 用途 | 面试加分点 |
|------|------|-----------|
| **Hutool** | Java工具类 | 提升开发效率 |
| **Guava** | 集合、缓存工具 | 本地缓存、限流算法 |
| **Lombok** | ✅ 已有 | 简化代码 |
| **MapStruct** | 对象映射 | 替代BeanUtils，性能更好 |
| **Swagger / Knife4j** | API文档 | 接口规范化 |
| **Jackson** | JSON处理 | 序列化配置 |
| **Easy Excel** | Excel导入导出 | 报表功能 |

### 3.4 测试与监控

| 技术 | 用途 | 面试加分点 |
|------|------|-----------|
| **JUnit 5** | 单元测试 | 单元测试覆盖率 |
| **Mockito** | Mock测试 | 测试驱动开发 |
| **JMeter** | 压力测试 | 性能调优 |
| **Spring Boot Actuator** | 健康检查 | 应用监控 |
| **Prometheus + Grafana** | 监控大盘 | 可观测性 |
| **ELK Stack** | 日志收集 | 分布式日志追踪 |
| **SkyWalking** | 链路追踪 | APM性能监控 |

---

## 四、架构设计方案

### 4.1 分层架构（推荐单体应用分层）

```
┌─────────────────────────────────────────────────────────┐
│                     Controller Layer                     │
│  （参数校验、权限校验、统一返回、异常处理）                  │
└───────────────────────┬─────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────┐
│                     Service Layer                        │
│  （业务逻辑、事务控制、缓存策略、分布式锁）                    │
│                                                          │
│  ├─ User Service        ├─ Order Service                │
│  ├─ Restaurant Service  ├─ AI Chat Service              │
│  ├─ Review Service      └─ Recommendation Service       │
└───────────┬────────────────────────┬────────────────────┘
            │                        │
┌───────────▼────────────┐  ┌────────▼───────────────────┐
│    Mapper Layer        │  │   External Integration     │
│  （MyBatis-Plus）       │  │   ├─ LangChain4j (AI)      │
│  ├─ User Mapper        │  │   ├─ Redis (Cache)         │
│  ├─ Order Mapper       │  │   ├─ RabbitMQ (MQ)         │
│  ├─ Restaurant Mapper  │  │   ├─ Elasticsearch         │
│  └─ Review Mapper      │  │   └─ OSS (Storage)         │
└────────────────────────┘  └────────────────────────────┘
```

### 4.2 核心模块划分

```
src/main/java/cn/zjw/
├── common/                     # 公共模块
│   ├── constant/              # 常量定义
│   ├── enums/                 # 枚举类
│   ├── exception/             # 自定义异常
│   ├── result/                # 统一返回结果
│   └── utils/                 # 工具类
├── config/                    # 配置类
│   ├── RedisConfig            # Redis配置
│   ├── SecurityConfig         # Security配置
│   ├── WebMvcConfig           # MVC配置
│   ├── RabbitMQConfig         # MQ配置
│   ├── ESConfig               # ES配置
│   └── CommonConfig (AI配置)  # ✅ 已有
├── controller/                # 控制层
│   ├── user/
│   │   ├── UserController     # 用户信息
│   │   └── AuthController     # 登录注册
│   ├── restaurant/
│   │   ├── RestaurantController
│   │   └── DishController
│   ├── order/
│   │   └── OrderController
│   ├── review/
│   │   └── ReviewController
│   ├── chat/
│   │   └── ChatController     # ✅ 已有
│   └── admin/
│       └── AdminController
├── service/                   # 业务层
│   ├── user/
│   │   ├── UserService
│   │   └── AuthService
│   ├── restaurant/
│   │   ├── RestaurantService
│   │   └── DishService
│   ├── order/
│   │   └── OrderService
│   ├── review/
│   │   └── ReviewService
│   ├── ai/
│   │   ├── ConsultantService  # ✅ 已有
│   │   └── RecommendService
│   └── cache/
│       └── CacheService
├── mapper/                    # 数据访问层
│   ├── UserMapper
│   ├── RestaurantMapper
│   ├── OrderMapper
│   └── ReviewMapper
├── pojo/                      # 实体类
│   ├── entity/               # 数据库实体
│   ├── dto/                  # 数据传输对象
│   └── vo/                   # 视图对象
├── interceptor/              # 拦截器
│   ├── AuthInterceptor       # 登录校验
│   └── RateLimitInterceptor  # 限流拦截
├── aspect/                   # AOP切面
│   ├── LogAspect             # 日志切面
│   └── CacheAspect           # 缓存切面
├── handler/                  # 处理器
│   ├── GlobalExceptionHandler # 全局异常
│   └── MyMetaObjectHandler   # MyBatis自动填充
├── task/                     # 定时任务
│   ├── OrderTimeoutTask      # 订单超时处理
│   └── HotRankTask           # 热榜计算
└── tools/                    # AI工具
    └── FoodReservationTool   # ✅ 已有
```

### 4.3 数据库表设计（核心表）

```sql
-- 用户表
CREATE TABLE `user` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `username` VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
  `password` VARCHAR(255) COMMENT '密码（加密）',
  `phone` VARCHAR(11) UNIQUE COMMENT '手机号',
  `nickname` VARCHAR(50) COMMENT '昵称',
  `avatar` VARCHAR(500) COMMENT '头像URL',
  `status` TINYINT DEFAULT 1 COMMENT '状态：0-禁用，1-启用',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 餐厅表
CREATE TABLE `restaurant` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `name` VARCHAR(200) NOT NULL COMMENT '餐厅名称',
  `category` VARCHAR(50) COMMENT '菜系分类',
  `address` VARCHAR(500) COMMENT '详细地址',
  `latitude` DECIMAL(10,6) COMMENT '纬度',
  `longitude` DECIMAL(10,6) COMMENT '经度',
  `avg_price` DECIMAL(10,2) COMMENT '人均消费',
  `rating` DECIMAL(3,2) DEFAULT 5.0 COMMENT '评分',
  `phone` VARCHAR(20) COMMENT '联系电话',
  `business_hours` VARCHAR(200) COMMENT '营业时间',
  `images` TEXT COMMENT '图片URL列表（JSON）',
  `status` TINYINT DEFAULT 1 COMMENT '状态：0-停业，1-营业',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_category (category),
  INDEX idx_rating (rating),
  SPATIAL INDEX idx_location (location) -- 地理位置索引
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='餐厅表';

-- 订单表
CREATE TABLE `order` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `order_no` VARCHAR(32) NOT NULL UNIQUE COMMENT '订单号',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `restaurant_id` BIGINT NOT NULL COMMENT '餐厅ID',
  `reservation_time` DATETIME NOT NULL COMMENT '预订时间',
  `people_count` INT NOT NULL COMMENT '用餐人数',
  `contact_name` VARCHAR(100) COMMENT '联系人',
  `contact_phone` VARCHAR(20) COMMENT '联系电话',
  `special_request` TEXT COMMENT '特殊要求',
  `status` TINYINT DEFAULT 0 COMMENT '状态：0-待确认，1-已确认，2-已完成，3-已取消',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_user (user_id),
  INDEX idx_restaurant (restaurant_id),
  INDEX idx_status (status),
  INDEX idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';

-- 评价表
CREATE TABLE `review` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `restaurant_id` BIGINT NOT NULL COMMENT '餐厅ID',
  `order_id` BIGINT COMMENT '订单ID',
  `rating` TINYINT NOT NULL COMMENT '评分：1-5',
  `content` TEXT COMMENT '评价内容',
  `images` TEXT COMMENT '图片URL列表（JSON）',
  `status` TINYINT DEFAULT 0 COMMENT '审核状态：0-待审核，1-已通过，2-已拒绝',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_user (user_id),
  INDEX idx_restaurant (restaurant_id),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评价表';

-- 用户标签表（饮食偏好）
CREATE TABLE `user_tag` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `tag_type` VARCHAR(20) COMMENT '标签类型：taste-口味，allergy-过敏源，diet-饮食习惯',
  `tag_value` VARCHAR(50) COMMENT '标签值',
  UNIQUE KEY uk_user_tag (user_id, tag_type, tag_value)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户标签表';

-- 其他表：dish(菜品)、favorite(收藏)、user_follow(关注)、post(动态) 等
```

---

## 五、重点技术功能设计

### 5.1 🔥 高频面试技术点（必须实现）

#### 1. Redis缓存设计

**缓存场景设计**:
```java
// 1. 缓存更新策略（Cache-Aside Pattern）
@Service
public class RestaurantService {
    // 查询餐厅信息（缓存）
    public Restaurant getById(Long id) {
        // 1. 先查缓存
        String key = "restaurant:" + id;
        Restaurant restaurant = redisTemplate.opsForValue().get(key);

        // 2. 缓存未命中，查数据库
        if (restaurant == null) {
            restaurant = restaurantMapper.selectById(id);
            if (restaurant != null) {
                // 3. 写入缓存，设置过期时间（防止缓存雪崩加随机值）
                redisTemplate.opsForValue().set(key, restaurant,
                    30 + RandomUtil.randomInt(10), TimeUnit.MINUTES);
            } else {
                // 4. 防止缓存穿透：缓存空对象
                redisTemplate.opsForValue().set(key, new Restaurant(),
                    5, TimeUnit.MINUTES);
            }
        }
        return restaurant;
    }

    // 更新餐厅信息（先删缓存，再更新数据库）
    @Transactional
    public void update(Restaurant restaurant) {
        // 1. 删除缓存
        redisTemplate.delete("restaurant:" + restaurant.getId());
        // 2. 更新数据库
        restaurantMapper.updateById(restaurant);
    }
}
```

**布隆过滤器防止缓存穿透**:
```java
@Configuration
public class RedisConfig {
    @Bean
    public RBloomFilter<String> bloomFilter(RedissonClient redissonClient) {
        RBloomFilter<String> bloomFilter = redissonClient.getBloomFilter("restaurant:bloom");
        // 初始化：预期100万数据，误判率0.01
        bloomFilter.tryInit(1000000L, 0.01);
        return bloomFilter;
    }
}

@Service
public class RestaurantService {
    @Autowired
    private RBloomFilter<String> bloomFilter;

    public Restaurant getById(Long id) {
        String key = "restaurant:" + id;

        // 布隆过滤器判断是否存在
        if (!bloomFilter.contains(key)) {
            return null; // 一定不存在，直接返回
        }

        // 可能存在，继续查询缓存和数据库
        // ... 后续逻辑
    }
}
```

**分布式锁解决缓存击穿**:
```java
@Service
public class RestaurantService {
    public Restaurant getById(Long id) {
        String cacheKey = "restaurant:" + id;
        Restaurant restaurant = redisTemplate.opsForValue().get(cacheKey);

        if (restaurant == null) {
            // 使用Redisson分布式锁
            String lockKey = "lock:restaurant:" + id;
            RLock lock = redissonClient.getLock(lockKey);

            try {
                // 尝试加锁，最多等待10秒，锁30秒后自动释放
                if (lock.tryLock(10, 30, TimeUnit.SECONDS)) {
                    try {
                        // 双重检查
                        restaurant = redisTemplate.opsForValue().get(cacheKey);
                        if (restaurant == null) {
                            // 查询数据库
                            restaurant = restaurantMapper.selectById(id);
                            redisTemplate.opsForValue().set(cacheKey, restaurant, 30, TimeUnit.MINUTES);
                        }
                    } finally {
                        lock.unlock();
                    }
                }
            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
            }
        }
        return restaurant;
    }
}
```

#### 2. 消息队列异步处理

**场景：订单创建后发送短信通知**

```java
// 生产者：发送消息
@Service
public class OrderService {
    @Autowired
    private RabbitTemplate rabbitTemplate;

    @Transactional
    public Order createOrder(OrderDTO orderDTO) {
        // 1. 创建订单
        Order order = new Order();
        // ... 设置订单信息
        orderMapper.insert(order);

        // 2. 发送MQ消息（异步通知）
        OrderMessageDTO message = new OrderMessageDTO();
        message.setOrderId(order.getId());
        message.setPhone(order.getContactPhone());
        rabbitTemplate.convertAndSend("order.exchange", "order.create", message);

        return order;
    }
}

// 消费者：接收消息并发送短信
@Component
public class OrderMessageConsumer {

    @RabbitListener(queues = "order.create.queue")
    public void handleOrderCreate(OrderMessageDTO message) {
        // 幂等性校验（防止重复消费）
        String idempotentKey = "order:sms:" + message.getOrderId();
        Boolean success = redisTemplate.opsForValue().setIfAbsent(
            idempotentKey, "1", 24, TimeUnit.HOURS);

        if (Boolean.TRUE.equals(success)) {
            // 发送短信
            smsService.sendOrderConfirmSms(message.getPhone(), message.getOrderId());
        }
    }
}
```

#### 3. 接口限流

**Sentinel实现接口限流**:
```java
@RestController
@RequestMapping("/api/restaurant")
public class RestaurantController {

    // 使用Sentinel注解限流：QPS=10
    @SentinelResource(
        value = "getRestaurantById",
        blockHandler = "handleBlock",
        fallback = "handleFallback"
    )
    @GetMapping("/{id}")
    public Result<Restaurant> getById(@PathVariable Long id) {
        return Result.success(restaurantService.getById(id));
    }

    // 限流后的降级方法
    public Result<Restaurant> handleBlock(Long id, BlockException e) {
        return Result.error("系统繁忙，请稍后再试");
    }

    // 异常时的降级方法
    public Result<Restaurant> handleFallback(Long id, Throwable e) {
        return Result.error("服务异常");
    }
}
```

**Redis + Lua实现滑动窗口限流**:
```java
@Component
public class RateLimitInterceptor implements HandlerInterceptor {

    @Override
    public boolean preHandle(HttpServletRequest request,
                            HttpServletResponse response,
                            Object handler) {
        String userId = request.getHeader("userId");
        String key = "rate:limit:" + userId;

        // Lua脚本实现滑动窗口（1分钟内最多100次请求）
        String luaScript = """
            local key = KEYS[1]
            local now = tonumber(ARGV[1])
            local window = tonumber(ARGV[2])
            local limit = tonumber(ARGV[3])

            redis.call('zremrangebyscore', key, 0, now - window)
            local count = redis.call('zcard', key)

            if count < limit then
                redis.call('zadd', key, now, now)
                redis.call('expire', key, window)
                return 1
            else
                return 0
            end
            """;

        Long result = redisTemplate.execute(
            new DefaultRedisScript<>(luaScript, Long.class),
            Collections.singletonList(key),
            System.currentTimeMillis(), 60000, 100
        );

        if (result == 0) {
            response.setStatus(429);
            return false;
        }
        return true;
    }
}
```

#### 4. Elasticsearch搜索

**餐厅搜索功能**:
```java
@Service
public class RestaurantSearchService {
    @Autowired
    private ElasticsearchRestTemplate esTemplate;

    public List<Restaurant> search(String keyword, String category,
                                   Double minPrice, Double maxPrice) {
        NativeSearchQueryBuilder builder = new NativeSearchQueryBuilder();

        BoolQueryBuilder boolQuery = QueryBuilders.boolQuery();

        // 1. 全文检索（餐厅名称、菜系）
        if (StrUtil.isNotBlank(keyword)) {
            boolQuery.should(QueryBuilders.matchQuery("name", keyword).boost(2.0f));
            boolQuery.should(QueryBuilders.matchQuery("category", keyword));
        }

        // 2. 精确过滤
        if (StrUtil.isNotBlank(category)) {
            boolQuery.filter(QueryBuilders.termQuery("category", category));
        }

        // 3. 范围查询
        if (minPrice != null || maxPrice != null) {
            RangeQueryBuilder rangeQuery = QueryBuilders.rangeQuery("avgPrice");
            if (minPrice != null) rangeQuery.gte(minPrice);
            if (maxPrice != null) rangeQuery.lte(maxPrice);
            boolQuery.filter(rangeQuery);
        }

        // 4. 按评分排序
        builder.withQuery(boolQuery)
               .withSort(SortBuilders.fieldSort("rating").order(SortOrder.DESC));

        SearchHits<Restaurant> hits = esTemplate.search(builder.build(), Restaurant.class);
        return hits.stream()
                   .map(SearchHit::getContent)
                   .collect(Collectors.toList());
    }
}
```

#### 5. 统一异常处理

```java
@RestControllerAdvice
@Slf4j
public class GlobalExceptionHandler {

    // 业务异常
    @ExceptionHandler(BusinessException.class)
    public Result<Void> handleBusinessException(BusinessException e) {
        log.error("业务异常：{}", e.getMessage());
        return Result.error(e.getCode(), e.getMessage());
    }

    // 参数校验异常
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<Void> handleValidException(MethodArgumentNotValidException e) {
        String message = e.getBindingResult().getFieldError().getDefaultMessage();
        return Result.error(400, message);
    }

    // 未授权异常
    @ExceptionHandler(UnauthorizedException.class)
    public Result<Void> handleUnauthorizedException(UnauthorizedException e) {
        return Result.error(401, "未登录或登录已过期");
    }

    // 系统异常
    @ExceptionHandler(Exception.class)
    public Result<Void> handleException(Exception e) {
        log.error("系统异常", e);
        return Result.error(500, "系统繁忙，请稍后再试");
    }
}
```

### 5.2 🌟 加分项技术点

#### 1. 定时任务：订单超时自动取消

```java
@Component
@Slf4j
public class OrderTimeoutTask {

    @XxlJob("orderTimeoutHandler")
    public void execute() {
        log.info("开始处理超时订单");

        // 查询30分钟前创建且仍为"待确认"状态的订单
        LocalDateTime timeout = LocalDateTime.now().minusMinutes(30);
        List<Order> timeoutOrders = orderMapper.selectList(
            new LambdaQueryWrapper<Order>()
                .eq(Order::getStatus, OrderStatus.PENDING)
                .lt(Order::getCreateTime, timeout)
        );

        // 批量取消订单
        if (CollUtil.isNotEmpty(timeoutOrders)) {
            List<Long> orderIds = timeoutOrders.stream()
                .map(Order::getId)
                .collect(Collectors.toList());

            orderMapper.update(null, new LambdaUpdateWrapper<Order>()
                .in(Order::getId, orderIds)
                .set(Order::getStatus, OrderStatus.CANCELLED)
            );

            log.info("已取消{}个超时订单", orderIds.size());
        }
    }
}
```

#### 2. AOP日志记录

```java
@Aspect
@Component
@Slf4j
public class LogAspect {

    @Around("@annotation(operationLog)")
    public Object around(ProceedingJoinPoint point, OperationLog operationLog) throws Throwable {
        long startTime = System.currentTimeMillis();

        // 获取请求信息
        ServletRequestAttributes attributes =
            (ServletRequestAttributes) RequestContextHolder.getRequestAttributes();
        HttpServletRequest request = attributes.getRequest();

        // 记录请求日志
        log.info("操作：{}，IP：{}，方法：{}，参数：{}",
            operationLog.value(),
            request.getRemoteAddr(),
            point.getSignature().toShortString(),
            JSON.toJSONString(point.getArgs())
        );

        Object result = point.proceed();

        long duration = System.currentTimeMillis() - startTime;
        log.info("操作完成，耗时：{}ms", duration);

        return result;
    }
}
```

#### 3. MyBatis-Plus自动填充

```java
@Component
public class MyMetaObjectHandler implements MetaObjectHandler {

    @Override
    public void insertFill(MetaObject metaObject) {
        this.strictInsertFill(metaObject, "createTime", LocalDateTime.class, LocalDateTime.now());
        this.strictInsertFill(metaObject, "updateTime", LocalDateTime.class, LocalDateTime.now());

        // 自动填充创建人ID（从ThreadLocal获取当前用户）
        Long userId = UserContext.getCurrentUserId();
        if (userId != null) {
            this.strictInsertFill(metaObject, "createUser", Long.class, userId);
        }
    }

    @Override
    public void updateFill(MetaObject metaObject) {
        this.strictUpdateFill(metaObject, "updateTime", LocalDateTime.class, LocalDateTime.now());
    }
}
```

---

## 六、面试重点关注技术

### 6.1 必须掌握的核心知识点

#### 1. **Spring Boot核心**
- 自动配置原理（@SpringBootApplication、@EnableAutoConfiguration）
- Starter机制
- 条件装配（@Conditional）
- 配置文件加载优先级
- Bean生命周期

#### 2. **Spring Security + JWT认证**
- 认证流程（UsernamePasswordAuthenticationFilter）
- 授权流程（FilterSecurityInterceptor）
- JWT token生成与验证
- 权限注解（@PreAuthorize）
- CORS跨域处理

#### 3. **MySQL优化**
- 索引原理（B+树）
- 索引失效场景
- 慢查询优化
- 事务隔离级别（MVCC）
- 锁机制（行锁、表锁、间隙锁）

#### 4. **Redis核心**
- 数据结构（String、Hash、List、Set、ZSet）
- 缓存更新策略（Cache-Aside、Read/Write Through）
- 缓存问题（穿透、击穿、雪崩）
- 分布式锁实现（Redisson）
- 持久化（RDB、AOF）
- 主从复制、哨兵、集群

#### 5. **消息队列**
- RabbitMQ核心概念（Exchange、Queue、Routing Key）
- 消息可靠性保证（ack、持久化、备份交换机）
- 消息幂等性设计
- 死信队列处理
- 延迟队列实现

#### 6. **MyBatis-Plus**
- 条件构造器（LambdaQueryWrapper）
- 分页插件
- 逻辑删除
- 自动填充
- 乐观锁插件

### 6.2 加分项技术

#### 1. **分布式理论**
- CAP理论
- BASE理论
- 分布式事务（Seata）
- 分布式ID生成（雪花算法）

#### 2. **高并发设计**
- 秒杀系统设计（令牌桶、库存预热、异步扣减）
- 热点数据处理
- 读写分离
- 分库分表（ShardingSphere）

#### 3. **微服务架构**
- Spring Cloud Alibaba（Nacos、Sentinel、Seata）
- 服务注册与发现
- 配置中心
- 负载均衡（Ribbon、LoadBalancer）
- 熔断降级（Sentinel）
- 网关（Gateway）

#### 4. **性能优化**
- JVM调优（堆内存、GC）
- 线程池调优
- 连接池调优（Druid）
- SQL优化
- 接口性能优化（异步化、批量化、缓存）

---

## 七、开发实施步骤建议

### 阶段一：基础重构（1-2周）

**目标**: 完善MVC架构，规范代码结构

1. **统一返回结果封装**
   - 创建Result、PageResult类
   - 统一异常处理

2. **完善用户体系**
   - 用户注册/登录（手机号+验证码）
   - JWT认证
   - 用户信息管理

3. **订单模块重构**
   - 完善订单状态流转
   - 订单查询（分页）
   - 订单取消功能

4. **MyBatis升级为MyBatis-Plus**
   - 使用LambdaQueryWrapper
   - 配置分页插件
   - 配置自动填充

**交付物**:
- 完整的MVC分层代码
- API接口文档（Swagger）
- 数据库表设计文档

---

### 阶段二：核心功能开发（2-3周）

**目标**: 实现核心业务功能

1. **餐厅管理模块**
   - 餐厅列表查询（分页、筛选）
   - 餐厅详情查询
   - 餐厅收藏功能

2. **评价模块**
   - 用户评价（文字+图片）
   - 评价列表查询
   - 评分统计

3. **AI推荐优化**
   - 基于用户标签的个性化推荐
   - 优化RAG知识库（增加更多美食知识）

4. **搜索功能**
   - 集成Elasticsearch
   - 餐厅全文搜索
   - 搜索关键词高亮

**交付物**:
- 核心功能代码
- 单元测试用例
- 功能测试报告

---

### 阶段三：中间件集成（2-3周）

**目标**: 集成主流中间件，提升系统能力

1. **Redis深度应用**
   - 缓存层设计（多级缓存）
   - 分布式锁（预订扣减库存）
   - 布隆过滤器（防止缓存穿透）
   - 限流器（接口限流）

2. **RabbitMQ集成**
   - 订单异步通知
   - 评价异步审核
   - 延迟队列（订单超时取消）

3. **定时任务**
   - XXL-Job部署
   - 订单超时处理任务
   - 热门榜单计算任务

4. **OSS集成**
   - 图片上传功能
   - 图片压缩优化

**交付物**:
- 中间件集成文档
- 性能测试报告（JMeter）
- 缓存设计文档

---

### 阶段四：优化与监控（1-2周）

**目标**: 性能优化、可观测性

1. **性能优化**
   - SQL优化（慢查询）
   - 接口性能优化
   - 缓存命中率优化

2. **监控体系**
   - Spring Boot Actuator健康检查
   - Prometheus + Grafana监控大盘
   - 日志收集（ELK可选）

3. **文档完善**
   - 项目README
   - 数据库设计文档
   - 架构设计文档
   - 部署文档

**交付物**:
- 性能优化报告
- 监控大盘
- 完整项目文档

---

## 八、面试准备建议

### 8.1 项目介绍话术模板

```
这是一个智能美食推荐与餐厅管理系统，核心特点是结合了AI大模型技术和传统业务系统。

【技术架构】
- 后端：Spring Boot 3.5 + MyBatis-Plus + Spring Security
- 中间件：Redis（缓存+分布式锁）、RabbitMQ（异步解耦）、Elasticsearch（搜索）
- AI模块：LangChain4j + 阿里云通义千问，实现了RAG知识库和智能对话
- 数据库：MySQL主库 + Redis缓存

【核心功能】
1. 用户端：AI美食顾问、个性化推荐、餐厅预订、评价系统
2. 商家端：餐厅管理、订单管理、数据统计
3. 平台端：用户管理、审核管理、系统监控

【技术亮点】
1. 缓存架构：实现了多级缓存，使用布隆过滤器防止缓存穿透，用Redisson分布式锁解决缓存击穿
2. 消息队列：用RabbitMQ实现订单异步通知，保证了消息的幂等性和可靠性
3. 搜索引擎：基于ES实现餐厅全文搜索，支持拼音搜索和模糊匹配
4. AI集成：使用LangChain4j的RAG技术，结合向量数据库实现智能问答
5. 限流降级：使用Sentinel实现接口限流和熔断降级

【性能指标】
- 接口平均响应时间：200ms以内
- 缓存命中率：95%+
- 支持QPS：1000+（压测数据）

【个人贡献】
独立完成整体架构设计和核心功能开发，重点负责缓存架构设计、消息队列集成、ES搜索优化。
```

### 8.2 高频面试题准备

**Redis相关**:
- 如何解决缓存穿透、击穿、雪崩？（结合项目实际代码）
- 分布式锁的实现原理？为什么选择Redisson？
- 缓存一致性如何保证？（Cache-Aside策略）

**消息队列相关**:
- 如何保证消息不丢失？（生产者确认、持久化、消费者ack）
- 如何保证消息不重复消费？（幂等性设计）
- 死信队列的使用场景？

**MySQL相关**:
- 订单表/评价表的索引如何设计？
- 如何优化慢查询？（举实际例子）
- 事务隔离级别的选择？

**系统设计相关**:
- 如何设计一个秒杀系统？
- 如何设计一个推荐系统？
- 如何保证接口幂等性？

---

## 九、技术选型总结表

| 分类 | 技术 | 优先级 | 作用 |
|------|------|--------|------|
| **基础框架** | Spring Boot 3.5 | ⭐⭐⭐⭐⭐ | 核心框架 |
| | Spring Security | ⭐⭐⭐⭐⭐ | 认证授权 |
| | MyBatis-Plus | ⭐⭐⭐⭐⭐ | ORM增强 |
| **数据库** | MySQL 8.0 | ⭐⭐⭐⭐⭐ | 关系型数据库 |
| | Redis 7.x | ⭐⭐⭐⭐⭐ | 缓存+分布式能力 |
| **中间件** | RabbitMQ | ⭐⭐⭐⭐ | 消息队列 |
| | Elasticsearch | ⭐⭐⭐⭐ | 搜索引擎 |
| | XXL-Job | ⭐⭐⭐ | 分布式定时任务 |
| | Sentinel | ⭐⭐⭐ | 流量控制 |
| | Nginx | ⭐⭐⭐ | 反向代理 |
| **工具库** | Redisson | ⭐⭐⭐⭐ | 分布式工具 |
| | Hutool | ⭐⭐⭐⭐ | Java工具类 |
| | Knife4j | ⭐⭐⭐ | API文档 |
| **AI相关** | LangChain4j | ⭐⭐⭐⭐⭐ | AI集成框架 |
| | 通义千问 | ⭐⭐⭐⭐⭐ | 大语言模型 |
| **监控** | Actuator | ⭐⭐⭐ | 健康检查 |
| | Prometheus + Grafana | ⭐⭐ | 监控大盘（可选） |

**优先级说明**:
- ⭐⭐⭐⭐⭐：必须掌握，高频面试点
- ⭐⭐⭐⭐：重要技术，建议实现
- ⭐⭐⭐：加分项，时间充裕可实现
- ⭐⭐：可选项，根据个人情况

---

## 十、预期成果

完成升级后，你将拥有：

1. **一个可运行的完整项目**
   - 10+ 张数据库表
   - 50+ 个API接口
   - 完整的前后端交互（保留现有Vue前端并扩展）

2. **技术深度与广度**
   - 掌握主流技术栈（Spring Boot、Redis、MQ、ES）
   - 理解分布式系统设计
   - 积累缓存、消息队列实战经验

3. **面试竞争力**
   - 可以讲出3-5个技术亮点
   - 能回答90%的技术细节问题
   - 有完整的系统设计思路

4. **开源项目经历**
   - 可以发布到GitHub（加Star）
   - 写技术博客（CSDN、掘金）
   - 制作项目演示视频

---

**祝你面试顺利！记住：项目要能运行，技术要能讲清，细节要能答上！** 🚀
